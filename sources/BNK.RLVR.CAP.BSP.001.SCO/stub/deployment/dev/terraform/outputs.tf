# BNK.RLVR.CAP.BSP.001.SCO contract+stub — Terraform outputs.
#
# Empty — the stub root provisions no resource (see main.tf rationale).
# Kept as a placeholder so the CI scaffold "terraform output -json" returns {}.
