# BNK.RLVR.CAP.SUP.002.BEN — stub — ServiceAccount (BASE).
#
# IRSA annotation is patched by overlay/dev/ once Terraform creates the
# corresponding IAM role per BNK.TECH.CAP.IDENTITY.001.WORKLOAD.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sup-002-ben-stub
  annotations:
    reliever.io/derived-from: BNK.TECH.CAP.IDENTITY.001.WORKLOAD
automountServiceAccountToken: true
