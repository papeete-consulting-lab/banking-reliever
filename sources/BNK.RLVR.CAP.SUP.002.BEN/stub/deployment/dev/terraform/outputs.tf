# Nothing to output until issue #7 lands a workload IRSA module and the
# associated secrets-binding module. Kept as a stub so the directory
# contract is satisfied and `terraform init` treats this as a valid root.
